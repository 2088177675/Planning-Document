# 公共竞赛 & 悬赏系统 + 自学习平台 — 系统规划文档

> 版本：v1.0（截至 2026-09-18）
> 部署形态：单机本地运行（Flask + SQLite + 本地 AI 模型）

---

## 一、项目概述

本系统对标 Kaggle 悬赏竞赛模式，整合「奖金托管竞赛 + 悬赏问答 + AI 自学习平台」三大业务，面向高校师生场景，强调资金安全、流程透明、AI 辅助学习。

### 核心定位
- **公共竞赛 & 悬赏**：会员前置托管奖金，全员免费参赛；系统自动结算（冠军全额发奖 / 流标自动退款）
- **AI 自学习平台**：学生与老师全员免费使用，依托本地部署的 Qwen2.5-1.5B 大模型，提供知识点生成、资料库管理、AI 助学、好友共享四大能力
- **资金安全**：平台不从奖金中抽成；所有资金流水全程记录、可审计

### 三类角色权限边界

| 角色 | 自学习平台 | 竞赛/悬赏发布 | 管理 |
|---|---|---|---|
| 学生 / 老师 | 全功能免费 | 仅会员可发布 | 不可 |
| 会员（叠加身份） | 同上 | 解锁发布权 | 不可 |
| 管理员 | 全功能 + AI 日志/配额/内容监管 | 不依赖会员身份 | 全权 |

> 会员体系与自学习平台**完全隔离**：会员仅解锁"发布公开竞赛/悬赏"权限，不影响自学习平台任何功能。

---

## 二、技术栈

### 后端
- **Web 框架**：Flask 2.3.3 + Werkzeug 2.3.7（无 ORM，原生 sqlite3）
- **数据库**：SQLite 3（单文件 `data/app.db`，零运维）
- **Python**：3.11.7（推荐用 Anaconda 或项目内 `.venv`）
- **架构**：蓝图解耦 — `main_bp / auth_bp / comp_bp / bounty_bp / admin_bp / learn_bp`
- **数据访问**：`db.py` 提供 `q() / qone() / execute()` 三件套，返回 `sqlite3.Row`
- **业务服务**：`services.py` 统一封装资金托管、自动评测、悬赏采纳、过期扫描

### 前端
- **框架**：Vue 3.4.21 UMD CDN + Element Plus 2 UMD CDN（无 Vite/Node 构建）
- **应用模式**：每页一个 Vue 实例（Options API），通过 `window.__bootVueApp()` 挂载
- **模板**：Jinja2 `base.html` 继承 + 各页 `templates/<page>.html`
- **状态注入**：Flask 通过 `window.__PAGE_DATA__` 注入数据，`window.__URLS__` 注入路由表
- **Markdown 渲染**：`marked` + `mermaid` CDN，`vue-boot.js` 提供全局 `__renderMarkdown`
- **表单提交**：登录/注册/发布/提交等仍走原生 form POST（保证简单可靠）

### AI 模型部署（本地，纯 CPU 可运行）
- **运行时**：`llama.cpp b11026`（llama-server.exe，17.6MB）
- **模型**：Qwen2.5-1.5B-Instruct q8_0 GGUF（1.76GB）；备用 0.5B（644MB）
- **接口**：OpenAI 兼容 `http://127.0.0.1:11434/v1/chat/completions`
- **性能**：纯 CPU ~11 tokens/s，单次摘要生成约 10-15 秒
- **启动**：双击 `ai_runtime/start_ai.bat`

### 运行环境
- 操作系统：Windows 10/11
- Python 解释器：`e:\zhuomian\公共竞赛和悬赏系统\.venv\Scripts\python.exe`
- 启动命令：`python -c "from app import app; app.run(host='127.0.0.1', port=5000, debug=True, use_reloader=False)"`
- **注意**：不能开 reloader（Anaconda 的 watchdog 版本太老会崩）

---

## 三、目录结构

```
公共竞赛和悬赏系统/
├── app.py                 # 入口 + main_bp（首页/个人中心/充值）+ create_app()
├── auth.py                # 认证蓝图（登录/注册/登出/会员申请）+ login_required
├── comp.py                # 竞赛蓝图（发布/列表/详情/报名/提交/管理/结算）
├── bounty.py              # 悬赏蓝图（发布/列表/详情/解答/采纳）
├── learn.py               # 自学习蓝图（AI生成/资料库/助学/好友/举报）
├── admin.py               # 管理后台蓝图（仪表盘/会员/资金/审核/AI监管）
├── services.py            # 业务服务（资金/结算/扫描/评分/采纳）
├── ai_service.py          # 本地 AI 调用封装（5 类生成模板 + 助学对话）
├── db.py                  # SQLite 连接 + 26 张表的 SCHEMA 定义
├── seed.py                # 演示数据种子（admin/teacher1/student1-3）
├── e2e_test.py            # 端到端自测脚本
├── files.py               # 文件辅助
│
├── templates/
│   ├── base.html          # 基础模板：导航栏 + Vue/Element Plus CDN + flash
│   ├── index.html         # 首页（hero + 统计 + 热门竞赛/悬赏）
│   ├── profile.html       # 个人中心（标签页：竞赛/悬赏/报名/解答/流水）
│   ├── auth/              # login.html / register.html
│   ├── comps/             # list / detail / publish / submit / manage
│   ├── bounties/          # list / detail / publish
│   ├── learn/             # index / library / material / explore / user / friends
│   └── admin/             # dashboard / members / funds / comps / bounties
│                           # archive / bank / learn_logs / learn_reports / learn_quota
│
├── static/
│   ├── vue-boot.js        # __bootVueApp 工具 + __COMP_STATUS__ / __BOUNTY_STATUS__
│   ├── style.css          # 全局样式 + Markdown 预览样式
│   └── views/             # 24 个 Vue 应用文件（每页一个 .js）
│
├── ai_runtime/            # 本地 AI 运行时（约 1.8GB）
│   ├── llama-server.exe   # OpenAI 兼容服务
│   ├── *.dll              # llama.cpp 依赖
│   ├── qwen2.5-1.5b-instruct-q8_0.gguf   # 主用模型
│   ├── qwen2.5-0.5b-instruct-q8_0.gguf   # 备用最小模型
│   └── start_ai.bat       # 启动脚本
│
├── data/
│   ├── app.db             # SQLite 数据库（单文件）
│   └── uploads/           # 文件上传目录
│
├── .venv/                 # Python 虚拟环境（Python 3.11.7 + Flask 2.3.3）
└── .idea/                 # PyCharm 配置
```

---

## 四、数据库设计（共 26 张表）

### 用户与会员
| 表 | 用途 |
|---|---|
| `users` | 用户主表（username/password_hash/role/is_member/balance） |
| `member_applications` | 会员申请记录（status: pending/approved/rejected） |
| `member_logs` | 会员操作日志（开通/撤销） |

### 竞赛模块
| 表 | 用途 |
|---|---|
| `competitions` | 竞赛主表（含奖金、评测模式、状态机、冠军字段） |
| `comp_materials` | 竞赛附件 |
| `comp_announcements` | 竞赛公告 |
| `comp_questions` | 自动评测题库（题号/答案/分值） |
| `teams` / `team_members` | 团队表 |
| `comp_signups` | 报名记录 |
| `submissions` | 参赛作品（含 answers_json 自动评测） |

### 悬赏模块
| 表 | 用途 |
|---|---|
| `bounties` | 悬赏主表（含赏金、到期时间、状态、采纳答案） |
| `bounty_attachments` | 悬赏附件 |
| `bounty_answers` | 解答记录（唯一约束：一人一答） |

### 资金模块
| 表 | 用途 |
|---|---|
| `transactions` | 资金流水（recharge/escrow/payout/refund + 方向 + 余额快照） |

### 自学习平台模块
| 表 | 用途 |
|---|---|
| `learn_folders` | 用户自定义文件夹 |
| `learn_materials` | AI 生成资料（含 kind/is_public/content Markdown） |
| `learn_uploads` | 用户上传文件 |
| `learn_ai_logs` | AI 调用日志（用户/类型/耗时/状态） |
| `learn_conversations` | AI 助学对话记录 |
| `user_follows` | 关注关系（follower_id → followee_id） |
| `user_favorites` | 资料收藏 |
| `content_reports` | 内容举报 |
| `user_quota` | 每日 AI 生成配额（默认 30/天） |

### 题库与归档
| 表 | 用途 |
|---|---|
| `question_bank` | 竞赛题目归档（可跨竞赛复用） |

---

## 五、功能模块详细

### 模块 A：公共竞赛

**业务流程：**
1. 会员发布 → 奖金全额托管扣款 → 状态 `pending`
2. 管理员审核 → 通过转 `open` / 驳回退款转 `rejected`
3. 报名期 → 报名/组队
4. 比赛期 → 提交作品（可多次提交，取最新版本）
5. 截止后 → 状态 `ended`（`services.sweep()` 自动扫描）
6. 结算：
   - 自动评测：按 `comp_questions` 标准答案判分，同分取最早提交
   - 人工评测：发布者打分后确认冠军
   - 流标：无有效作品，发布者主动判定，奖金退款
7. 冠军产生 → 状态 `settled`，奖金全额发放

**竞赛状态机：**
```
pending → open → ended → settled (冠军) / failed (流标)
pending → rejected (审核驳回，退款)
open/ended → terminated (管理员终止，退款)
```

**特殊约束：**
- 发布者不能自问自答（不能报名自己的竞赛）
- 竞赛发布时奖金需**全额托管**（余额不足则发布失败）
- 自动评测按客观题答案判分、同分取最早提交

### 模块 B：悬赏问答

**业务流程：**
1. 会员发布悬赏 → 赏金托管 → 状态 `pending`
2. 管理员审核 → 通过 `open` / 驳回退款 `rejected`
3. 全员解答（一人一答，可附文件）
4. 发布者采纳 → 状态 `resolved`，赏金发放给解答者
5. 到期无采纳 → `services.sweep()` 自动转 `expired` 并退款

### 模块 C：自学习平台（核心）

#### C1. AI 知识点智能生成
- 用户输入提示词 + 学科 + 难度（入门/常规/考试冲刺）
- 可手动指定输出类型：`ai_video_script`（视频素材包）/ `ai_mindmap`（讲义+思维导图）/ `ai_doc`（讲义）/ `ai_summary`（摘要）/ `ai_quiz`（练习题）
- 自动模式：`auto_decide_kind()` 让 AI 判断体量 → small 生成视频脚本 / system 生成讲义+思维导图
- 所有调用计入 `learn_ai_logs`，受 `user_quota` 每日配额限制（默认 30 次）

#### C2. 个人学习资料库
- AI 生成资料自动入库（私有）
- 文件夹自定义分类（新建/重命名/删除）
- 资料操作：预览（Markdown + Mermaid 渲染）/ 编辑 / 重命名 / 删除 / 收藏
- 公开/私有切换：私有严格隔离，公开后所有用户可见可收藏

#### C3. 沉浸式 AI 实时助学
- 查看任意资料时唤起侧边 AI 助手
- AI 自动读取当前资料作为上下文，仅针对当前知识点作答
- 支持答疑/扩写/精简/出题/总结等操作
- 对话记录保存到 `learn_conversations`，可回看复盘

#### C4. 好友体系 & 知识共享
- 用户搜索（`/learn/users/search`）→ 关注/取关（同一接口 toggle）
- 互相关注 = 双向好友
- 个人主页展示关注数/粉丝数/公开资料
- 公开广场 `/learn/explore` 浏览他人公开资料
- 违规举报 → 管理员下架

### 模块 D：管理后台

- **仪表盘** `/admin/`：平台数据总览
- **会员管理** `/admin/members`：审核会员申请/撤销会员
- **资金管理** `/admin/funds`：托管资金总额、流水审计
- **竞赛审核** `/admin/competitions`：审核/强制终止
- **悬赏审核** `/admin/bounties`：审核/强制终止
- **归档** `/admin/archive`：历史竞赛/悬赏
- **题库** `/admin/bank`：跨竞赛复用题目
- **AI 日志** `/admin/learn/logs`：AI 调用统计与明细
- **内容监管** `/admin/learn/reports`：处理举报、下架违规资料
- **配额管理** `/admin/learn/quota`：调整用户每日 AI 生成上限

### 模块 E：资金规则（硬约束）

- 平台**不从奖金中抽成**
- 竞赛发布时奖金**全额托管**
- 流标自动退款 / 悬赏到期无采纳自动退款
- 审核驳回/管理员下架均自动退款
- 冠军全额发奖
- 所有资金变动写入 `transactions` 流水（含类型、方向、余额快照、备注）

### 模块 F：AI 服务（ai_service.py）

- **模型**：Qwen2.5-1.5B-Instruct（本地，纯 CPU）
- **接口**：OpenAI 兼容 `http://127.0.0.1:11434/v1/chat/completions`
- **超时**：300 秒（本地 CPU 推理慢，给足时间）
- **5 类生成模板**：PROMPT_TEMPLATES 字典，按 kind 路由
- **自动体量判断**：AUTO_KIND_RULE 让模型输出 JSON `{"kind":"small|system"}`
- **助学对话**：ASSIST_SYSTEM_PROMPT 约束 AI 仅依据资料上下文作答
- **日志**：每次调用记 user_id / action / cost_ms / status / error

---

## 六、硬约束清单（来自项目记忆）

| 约束 | 说明 |
|---|---|
| 前端框架 | 必须 Vue 3 + Element Plus |
| 后端框架 | 必须 Flask 2.3 + SQLite |
| 文件上传 | 本地存储（`data/uploads/`） |
| 资金记录 | 全程流水，含余额快照 |
| 平台抽成 | 不抽成，奖金全额发放 |
| 发布者参赛 | 不能自问自答 |
| 管理员会员 | 永久会员身份 |
| 竞赛托管 | 发布时奖金全额托管 |
| 流标退款 | 自动退款 |
| 冠军发奖 | 全额发放 |
| 悬赏过期 | 无采纳自动过期退款 |
| 审核驳回 | 自动退款 |
| 会员档案 | 名单/撤销/日志存档 |
| 自动评测 | 客观题判分，同分取最早提交 |
| AI 模型 | 仅本地，无云端 API Key |

---

## 七、部署与运行

### 首次部署
1. 解压项目到 `e:\zhuomian\公共竞赛和悬赏系统\`
2. 创建虚拟环境：`E:\Anaconda\python.exe -m venv .venv`
3. 激活并装依赖：`.venv\Scripts\pip install Flask==2.3.3 Werkzeug==2.3.7 requests`
4. 初始化数据库：`.venv\Scripts\python.exe seed.py`（自动建表 + 演示账号）
5. 启动 AI 服务：双击 `ai_runtime\start_ai.bat`（保持窗口开着）
6. 启动 Flask：`.venv\Scripts\python.exe -c "from app import app; app.run(host='127.0.0.1', port=5000, debug=True, use_reloader=False)"`
7. 访问 http://127.0.0.1:5000/

### 演示账号
| 账号 | 密码 | 角色 |
|---|---|---|
| admin | admin123 | 管理员 |
| teacher1 | 123456 | 老师（会员） |
| student1 | 123456 | 学生 |
| student2 | 123456 | 学生 |
| student3 | 123456 | 学生 |

### 日常启动顺序
1. 先开 AI 服务（`start_ai.bat`）
2. 再开 Flask（`app.py`）
3. 两个窗口都要保持开着

---

## 八、路由清单（共 68 条）

详见 `app.url_map`，按蓝图分组：
- main_bp：`/`, `/profile`, `/recharge`
- auth_bp：`/login`, `/register`, `/logout`, `/member/apply`
- comp_bp：`/competitions/` 列表/详情/发布/报名/提交/管理/结算/组队
- bounty_bp：`/bounties/` 列表/详情/发布/解答/采纳/下载
- learn_bp：`/learn/` 首页/资料库/广场/资料详情/用户主页/好友/搜索/AI生成/助学/关注/收藏/举报/文件夹
- admin_bp：`/admin/` 仪表盘/会员/资金/竞赛审核/悬赏审核/归档/题库/AI日志/内容监管/配额

---

## 九、已知限制与后续规划

### 当前限制
1. **单机部署**：SQLite 不支持多机并发，适合小型教学场景（< 100 人同时在线）
2. **AI 性能**：1.5B 模型纯 CPU 11 tok/s，长讲义生成需 30-60 秒；复杂知识点质量有限
3. **无实时通信**：关注、解答采纳等无站内消息推送，需用户主动刷新
4. **支付模拟**：充值为模拟支付，仅用于演示托管流程
5. **无 HTTPS**：本地开发模式，生产部署需加反向代理 + TLS

### 后续可扩展方向
1. **模型升级**：换 3B/7B 模型，启用 GPU 加速（llama-server 加 `--n-gpu-layers`）
2. **多用户并发**：迁移 SQLite → PostgreSQL，加 Gunicorn 多 worker
3. **实时通知**：接入 WebSocket / Server-Sent Events 推送关注、采纳、审核结果
4. **支付集成**：对接支付宝/微信沙箱，替换模拟充值
5. **移动端**：当前为响应式 Web，可考虑 Taro 小程序跨端
6. **题库智能复用**：AI 根据竞赛主题自动从题库推荐相关题目
7. **学习路径推荐**：基于用户学习历史 + AI 推荐下一知识点
8. **团队协作学习**：学习资料共享给指定好友小组（非全公开）

---

## 十、关键经验教训

| 教训 | 说明 |
|---|---|
| 全局变量命名一致性 | `vue-boot.js` 与视图文件必须都用双下划线 `__COMP_STATUS__`，单双混用会导致 Vue 运行时报 `Cannot read properties of undefined` |
| Anaconda watchdog | 版本太老，Flask debug 模式不能开 reloader（`use_reloader=False`） |
| venv base 解释器 | `.venv` 依赖创建时的 base Python 路径，base 被删除/移动后 venv 启动器报退出码 103，需重建 |
| sqlite3.Row 列访问 | `qone()` 返回的 Row 只能访问 SELECT 中列出的字段，未查询字段访问会抛 `IndexError`（如收藏接口曾因只查 `id,user_id` 却访问 `is_public` 崩溃） |
| 模型下载源 | 国内环境 ModelScope 速度快（~5MB/s），Ollama 官方源和 GitHub 直链极慢（~0.1MB/s） |
| 本地 AI 超时 | 纯 CPU 推理慢，`requests.post` 超时需设 300 秒，默认 60 秒会中断长讲义生成 |

---

*文档结束。*
